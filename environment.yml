name: nanogpt
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - pytorch           # macOS/ARM build includes MPS
  - torchvision
  - torchaudio
  - numpy
  - transformers
  - tokenizers
  - datasets
  - tiktoken
  - tqdm
  - pip
  - pip:
      - wandb        # pip is usually fresher; fine to keep here
